## README

这是一个使用 react-virtualized 实现的虚拟列表组件示例项目。该项目展示了如何高效地渲染大量数据，同时保持良好的性能和用户体验。

### 功能特点

- **虚拟滚动**：仅渲染视口内可见的列表项，显著提升大数据集渲染性能
- **动态行高**：支持不同列表项具有不同的高度
- **响应式布局**：自动适应容器尺寸变化
- **可配置选项**：支持实时调整列表参数（行数、高度、滚动位置等）
